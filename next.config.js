/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
        images: {
    domains: ['www.friendlypartyrental.com','friendlypartyrental.com','images.unsplash.com'],
      },
}
module.exports = nextConfig
