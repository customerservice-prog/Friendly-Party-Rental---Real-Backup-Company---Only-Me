[build]
builder = "NIXPACKS"

[deploy]
startCommand = "sh -c 'npx prisma migrate deploy && exec node node_modules/next/dist/bin/next start -H 0.0.0.0 -p ${PORT:-8080}'"
restartPolicyType = "ON_FAILURE"
